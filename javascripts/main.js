requirejs(
  ["dom-access", "populate-songs", "get-more-songs"],
  function(dom, pop, more) {
  }

  // this should then put the returned objects from all three dependencies to
  //populate the song list
);